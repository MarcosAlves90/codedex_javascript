// Write code below 💖

const car = {
    model : "Tesla",
    year : "2023",
    color : "red",
    used : false
}

console.log(`I'm looking for a ${car.year} ${car.model} that is ${car.used ? "used." : "new."}`)